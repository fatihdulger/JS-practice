
/*let countInc = document.getElementById("pCount")
let counter = 0

function increment(){
    //counter = counter + 1  >>> we may use this way or below way
    counter++;
    countInc.innerText = counter;
    if(counter = 10) {
        setInterval(decrease, 2000)
    } else if(counter = 0) {
        clearInterval(decrease)
    } else { return}


    
    };
  



function decrease() {
    counter--;
    countInc.innerText = counter;
    
};

function reset() {
    counter = 0;
    countInc.innerHTML = counter;
    
};

let decrement = document.getElementById("pCount")
// you can do this setInterval so it can act like 

setInterval(increment, 1000) 


